apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sshark-api.fullname" . }}-test-connection"
  labels:
    {{- include "sshark-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sshark-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
